spring:
  datasource:
    url: jdbc:mysql://localhost:3306/iot_app_database
    username: root
    password: SqL1234!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: sql_server
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  address: 192.168.0.106
  port: 8080

